-- =============================================================
--  0028 — 마일스톤(홈 타임라인) 장소 필드
-- =============================================================
-- 홈 히어로 타임라인이 milestones 를 그대로 표시하므로, 노드별 장소(선택)를 둔다.
alter table milestones add column if not exists place text;

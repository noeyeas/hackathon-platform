-- =============================================================
--  0020 — 팀장 이메일로 팀장 자동 연결 (참가 코드 대체)
-- =============================================================
-- 구글폼에서 받은 팀장 이메일을 팀에 등록해 두면,
-- 해당 이메일로 로그인한 사용자를 앱에서 자동으로 그 팀의 팀장으로 연결한다.
alter table teams add column if not exists leader_email text;

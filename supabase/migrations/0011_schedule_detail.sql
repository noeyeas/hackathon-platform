-- =============================================================
--  0011 — 일정 항목 상세(토글로 펼쳐 보는 세부 일정)
-- =============================================================

alter table schedule_items add column if not exists detail text;
